apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Minimal reference overlay for vanilla Kubernetes. Copy this directory
# and adapt for your cluster.

namespace: multi-agent-commag

resources:
  - ../../core
