# Placeholder secret. Overlays MUST patch this with real values
# (see overlays/openshift-dev/secret-patch.yaml.example).
apiVersion: v1
kind: Secret
metadata:
  name: agents-secrets
type: Opaque
stringData:
  INFLUXDB_TOKEN: "agentran-dev-token"
